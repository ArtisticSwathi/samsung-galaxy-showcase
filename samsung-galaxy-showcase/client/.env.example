# Client environment variables
# Copy this file to .env.local and fill in your values

# URL of the deployed backend API (no trailing slash)
# Development: leave empty (Vite proxy will handle /api requests to localhost:5000)
# Production: set to your deployed backend URL, e.g. https://samsung-galaxy-showcase-api.onrender.com/api
VITE_API_URL=
